import {createTheme} from "@mui/material";

export const frontTheme = createTheme({
    // default colors
    palette: {
        primary: {
            main: '#a0acc3',
        },
        secondary: {
            main: '#dc004e',
        },
    },
    // default typography
    typography: {
        fontFamily: 'Roboto, sans-serif',
    },
})